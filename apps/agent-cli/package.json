{
  "name": "@pebble/agent-cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "pebble-agent-cli": "dist/main.js"
  },
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pebble/connector-core": "workspace:*",
    "@pebble/gateway": "workspace:*",
    "@pebble/protocol": "workspace:*",
    "commander": "^12.1.0",
    "eventsource": "^3.0.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
